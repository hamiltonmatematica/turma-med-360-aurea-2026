export interface ContactCardProps {
  initial: string;
  name: string;
  role: string;
  link: string;
  buttonText: string;
}

export interface BenefitItemProps {
  text: string;
}

export interface DetailBlockProps {
  title: string;
  items: string[];
}
